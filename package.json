{
  "name": "abstv",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Roku SceneGraph client for Audiobookshelf",
  "devDependencies": {
    "brighterscript": "0.73.0",
    "brighterscript-formatter": "1.8.2",
    "rimraf": "6.1.3",
    "roku-deploy": "3.16.1"
  },
  "scripts": {
    "clean": "npx rimraf build/ out/",
    "validate": "npx bsc --project bsconfig.json",
    "increment-build-version": "node ./scripts/increment-build-version.mjs",
    "build": "npm run increment-build-version && npm run clean && npm run validate",
    "package": "npm run build && node ./scripts/package.mjs",
    "deploy": "npm run increment-build-version && npm run validate && node ./scripts/deploy.mjs",
    "logviewer": "node ./scripts/logviewer.mjs"
  },
  "keywords": [
    "audiobookshelf",
    "roku",
    "audiobooks",
    "scenegraph"
  ],
  "license": "UNLICENSED"
}
